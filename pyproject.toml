[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
# N999: Invalid module name (允许 docstring 中的大小写)
# E501: Line too long (已忽略)
select = ["E", "F", "W", "I", "N", "UP", "B", "C4"]
ignore = ["E501", "N999"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]